import React from "react";
import { Link } from "react-router-dom";

export const MakeEntry = () => {
  return (
    <div>
      <h2>Make an entry</h2>
      <Link to="/">Back</Link>
    </div>
  );
};
